# No build-time environment variables are required.
#
# The contributors list and the GitHub star count are fetched client-side
# (browser) and cached for 24h in localStorage — nothing to configure here.
